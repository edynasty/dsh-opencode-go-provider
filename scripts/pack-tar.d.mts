/** Typed declaration for the shared tar validation (`pack-tar.mjs`). */
export function validateTarMembers(tarball: string): void;
export function extractTarballSafe(tarball: string, destDir: string): string;
